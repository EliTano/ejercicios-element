#include <stdio.h>

int main () {
    float not1, not2, prom;
    
    printf ("Ingresar notas del alumno: ");
    scanf ("%f%f", &not1, &not2);
    prom = (not1 + not2)/2;
    printf ("El promedio del alumno es %f", prom);
    
    return 0;
}
